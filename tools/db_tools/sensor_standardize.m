function [std_tb] = sensor_standardize(sdb_Sensors)
% SENSOR_STANDARDIZE Standardize the sensor database
%[sdb_standardized] = sensor_standardize(sdb_Sensors)

for idx = 1:size(sdb_Sensors,1)
    if ismissing(
end
